/**
 * 前端部署在 Netlify 等静态平台时，取消下一行注释并改成你的后端地址。
 * 后端需能跑 node server.js（Render / Railway / Fly.io / 自有 VPS）。
 *
 * 复制本文件为 config.js 再部署前端：
 *   copy config.example.js config.js
 */
// window.BANANA_API_BASE = 'https://your-banana-api.onrender.com';
