{
  "name": "banana-image-app",
  "version": "1.0.0",
  "private": true,
  "description": "Banana AI image generation UI + JWMP Gemini proxy",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "npm install --omit=dev"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.758.0"
  }
}
