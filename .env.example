# JWMP 平台 API Key（控制台 > API密钥）
JWMP_API_KEY=sk-xxxxxxxx

# 网关地址（一般不用改）
JWMP_BASE_URL=https://kwjm.com

# 本地服务端口
PORT=3780

# 数据目录（历史 + 图片 + 模型配置）。本地可省略，默认 ./data
# Render 挂载持久盘后请设为挂载点，例如：
# DATA_DIR=/var/data
# DATA_DIR=
